<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:WPFNode.Controls">

    <Style TargetType="{x:Type local:FlowPortControl}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:FlowPortControl}">
                    <Grid>
                        <Polygon x:Name="ShapePolygon"
                                Width="14"
                                Height="14"
                                Fill="{TemplateBinding Fill}"
                                Stroke="{TemplateBinding BorderBrush}"
                                StrokeThickness="{TemplateBinding BorderThickness}"
                                Points="0,7 7,0 14,7 7,14">
                            <Polygon.RenderTransform>
                                <RotateTransform Angle="{TemplateBinding RotationAngle}" 
                                                CenterX="7" CenterY="7"/>
                            </Polygon.RenderTransform>
                        </Polygon>
                        <TextBlock x:Name="PortName"
                                Text="{Binding Name}"
                                Foreground="{TemplateBinding Foreground}"
                                VerticalAlignment="Center"
                                HorizontalAlignment="{TemplateBinding TextAlignment}"
                                Margin="{TemplateBinding TextMargin}"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="ShapePolygon" Property="Fill" Value="{StaticResource {x:Static SystemColors.HighlightBrushKey}}" />
                        </Trigger>
                        <Trigger Property="IsConnected" Value="True">
                            <Setter TargetName="ShapePolygon" Property="Stroke" Value="#FF4CAF50" />
                            <Setter TargetName="ShapePolygon" Property="StrokeThickness" Value="2" />
                        </Trigger>
                        <Trigger Property="IsConnecting" Value="True">
                            <Setter TargetName="ShapePolygon" Property="Stroke" Value="#FFFFCC00" />
                            <Setter TargetName="ShapePolygon" Property="StrokeThickness" Value="2" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
