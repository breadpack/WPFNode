<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:WPFNode.Controls">

    <Style TargetType="{x:Type local:FlowConnectionControl}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:FlowConnectionControl}">
                    <Path x:Name="ConnectionPath"
                         Stroke="{TemplateBinding Stroke}"
                         StrokeThickness="{TemplateBinding StrokeThickness}"
                         StrokeDashArray="{TemplateBinding StrokeDashArray}"
                         Data="{Binding Path=PathGeometry}">
                        <Path.Effect>
                            <DropShadowEffect ShadowDepth="3" Opacity="0.3" BlurRadius="3" />
                        </Path.Effect>
                    </Path>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="ConnectionPath" Property="Stroke" Value="{StaticResource {x:Static SystemColors.HighlightBrushKey}}" />
                            <Setter TargetName="ConnectionPath" Property="StrokeThickness" Value="3" />
                        </Trigger>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="ConnectionPath" Property="Stroke" Value="#FFFF9800" />
                            <Setter TargetName="ConnectionPath" Property="StrokeThickness" Value="3" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
